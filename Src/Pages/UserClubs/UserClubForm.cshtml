@model UserClubFormModel

<div class="row">
    <div class="col-md">
        <div class="card card-body">
            <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center p-2 mb-4 border-bottom">
                <vc:legend title="Club Form" />
            </div>
            <form method="POST">
                <div class="mb-3">
                    <label for="user-club-name" class="mb-1">Name</label>
                    <input id="user-club-name" class="form-control"  asp-for="@Model.UserClub!.Name"/>
                    <span class="text-danger" asp-validation-for="@Model.UserClub!.Name"></span>
                </div>
                <div class="text-center">
                @if (ViewContext.RouteData.Values["id"] is null)
                {
                    <vc:submit-button label="Create" />
                } else
                {
                    <input hidden asp-for="@Model.UserClub!.Id" />
                    <vc:submit-button label="Update" />
                }
                </div>
            </form>
        </div>
    </div>
</div>